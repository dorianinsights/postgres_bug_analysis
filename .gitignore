venv/
.venv/
__pycache__/
.DS_Store
.env
# postgres.git bare clone (recreated by postgres_clone.py) + mbox archives
# (recreated by mailing_list_sync.py)
.cache/
# rendered dashboards (recreated by `dct render`) + dct serve state
out/
renders/
.dft/
.ruff_cache/
*.duckdb
# Temporary working files for duckdb
*.wal
# dbt scratch state (transform/ project)
transform/target/
transform/logs/
transform/dbt_packages/
/logs/

# local bug-report write-ups with rendered screenshots (never committed)
reports/

# Excluding third-party-added Claude skills for now
# Downside of this is that it would also exclude any custom skills you add to the repo, but we can revisit this if needed
.claude/skills/