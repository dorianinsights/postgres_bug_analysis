version: 2

models:
  - name: fct_wave_categories_agg
    description: >
      Tidy (release-wave, category) fix counts -> ../data/derived/fct_wave_categories_agg.csv,
      aggregated from fct_fixes and conformed to dim_release via dim_release_key.
      Only categories present in a wave are written, so fix_cnt >= 1.
      Grain = (dim_release_key, category).
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [dim_release_key, category]
    columns:
      - name: dim_release_key
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_release')
                field: dim_release_key
      - name: release_dt
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: "{{ var('test_floor_release_dt') }}"
                max_value: current_date
          - relationships:
              arguments:
                to: ref('dim_date')
                field: date_day
      - name: category
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('categories')
                field: category
      - name: category_order
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 9
      - name: fix_cnt
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1
      - name: is_out_of_band
        tests:
          - not_null
