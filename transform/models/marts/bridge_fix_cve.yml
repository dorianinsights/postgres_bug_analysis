version: 2

models:
  - name: bridge_fix_cve
    description: >
      Fix<->CVE bridge -> ../data/derived/bridge_fix_cve.csv: one row per
      (fix, CVE). Grain = (item_ord, dim_cve_key).
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [item_ord, dim_cve_key]
    columns:
      - name: item_ord
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('fct_fixes')
                field: item_ord
      - name: dim_cve_key
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_cve')
                field: dim_cve_key
