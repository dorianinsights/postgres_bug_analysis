version: 2

models:
  - name: fct_commits
    description: >
      Commit-grain fact -> ../data/derived/fct_commits.csv: FKs into dim_person
      (author + committer) and dim_date (commit day), diff-size measures, and
      origin/plumbing/AI degenerate attributes. Grain = (branch, commit_hash).
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [branch, commit_hash]
    columns:
      - name: author_person_key
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_person')
                field: person_key
      - name: committer_person_key
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_person')
                field: person_key
      - name: commit_date_key
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('dim_date')
                field: date_key
      - name: origin
        tests: [not_null]
      - name: is_plumbing
        tests: [not_null]
      - name: has_ai_credit
        tests: [not_null]
      - name: file_cnt
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
      - name: lines_added_sum
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
      - name: lines_deleted_sum
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
