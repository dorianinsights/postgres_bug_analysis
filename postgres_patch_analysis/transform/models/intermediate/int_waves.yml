version: 2

models:
  - name: int_waves
    description: One same-day release wave with its flags. Grain = wave_date.
    columns:
      - name: wave_date
        tests: [unique, not_null]
      - name: versions
        tests: [not_null]
